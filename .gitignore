.DS_Store
.git/

# R
.Rhistory
.RData
Rplots.pdf
.Rproj.user/
renv/library/

# Python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# Secrets / keys (never commit)
upload_key.json

# Data (downloadable / regenerable)
data/raw/**
data/processed/**
!data/raw/.gitkeep
!data/processed/.gitkeep

# Generated outputs (local runs; not committed)
results/metadata/**
results/causal/opengwas/**
results/causal/mr/**

# Local caches (regenerable)
data/references/ensembl/**
!data/references/ensembl/key_hits.tsv

# Runtime logs / scratch (never commit)
logs/**
evidence/**
trash/**
docs/audit_runs/**
docs/review_bundle/**

# Local-only manuscript generation helpers (do not commit)
scripts/08_update_manuscript.R

# Local runbooks / scratch scripts
run_plan_*.sh

# Office temp files
.~*
~$*

# Optional local exports (do not commit)
docs/MANUSCRIPT_ONLINE.*

# Debug artifacts
plots/publication/png/*debug*
scripts/audit/__pycache__/

# Legacy/experimental work (not part of reviewer-facing pipeline)
data/references/tcm/**
DATA_MANIFEST.md
extracted_refs.txt
results/figures/figure_legends_draft.md
docs/COVER_LETTER.md
docs/FIGURE_STYLE_NOTES.md
docs/references.bib
docs/vancouver.csl

# Large intermediate per-cell outputs (not needed for figure regeneration)
results/scpagwas/**/**singlecell_scPagwas_score_pvalue.Result.csv

# Ad-hoc root scripts (keep pipeline scripts under scripts/)
patch_*.sh
final_*.sh
resume_*.sh
rebuild_*.sh
execute_*pipeline*.sh
